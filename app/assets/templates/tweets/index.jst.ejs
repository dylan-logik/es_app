<div class='tweets'>
  <h1>Tweets</h1>
  <table id="tweets-list">
    <thead>
      <tr>
        <th>User</th>
        <th>Content</th>
      </tr>
    </thead>
    <tbody>
    </tbody>
  </table>
</div>
<div class='pagination'>
  <%= page_info.range[1] %> of <%= page_info.total %>
  <% if(page_info.next) { %>
    <a href="#" class="next">next</a>
  <% } else { %>
    <span>next</span>
  <% } %>
</div>
